### ✅ End of Day 2 Summary (NumPy Basics)

#### 🧠 Concepts Learned:

* Created arrays using `np.array`, `np.arange`, `np.linspace`, `np.zeros`, `np.ones`, `np.random`, etc.
* Understood `.shape`, `.dtype`, `.ndim`, `.size`
* Used `.reshape()` to change dimensions
* Practiced indexing & slicing with single/multi-dimensional arrays
* Mastered boolean indexing: filtering values by condition
* Performed math operations: `+`, `*`, `/`, `.sum()`, `.mean()`, `.max()` etc.
* Worked with 2D arrays: axis-based operations, reshaping
* Wrote and tested multiple small utility functions (like row normalization, edge highlighting)

#### ✅ Daily Checklist

| משימה                        | סטטוס |
| ---------------------------- | ----- |
| התקנתי numpy                 | ✅     |
| יודע ליצור arrays ב-10 דרכים | ✅     |
| מבין shape, dtype, reshape   | ✅     |
| שולט ב-indexing & slicing    | ✅     |
| שולט ב-boolean indexing      | ✅     |
| יודע פעולות מתמטיות בסיסיות  | ✅     |
| פתרתי את התרגולים            | ✅     |
| הכל ב-commit                 |      |
